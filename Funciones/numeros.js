function validarNumero(num) {
    if (num > 50) {
      console.log(" El numero es mayor a 50");
    } else {
      console.log("El numero es menor a 50");
    }
  }
  const numeroUno = parseInt(prompt("Ingresa el numero "));
  validarNumero(numeroUno);
  //const numeroDos = parseInt(prompt("Ingresa el numero "));
  //validarNumero(numeroDos);
  
  const ejemploNumero = 72
  validarNumero (ejemploNumero);
  
  validarNumero(4);

  
function sumaNumeros(numeroUno, numeroDos) {

let resultado = numeroUno + numeroDos
    console.log(resultado)

}

sumaNumeros( 2 , 3)

